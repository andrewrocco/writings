import React from 'react';
import { Link } from 'gatsby';

const Navigation = () => {
  return (
    <nav className="py-4">
      <Link to={"/"}>Home</Link>
    </nav>
  );
};

export default Navigation;
